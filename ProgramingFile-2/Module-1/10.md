

# ⭐ UNION TYPE ( | )

Union মানে — **একাধিক টাইপের যেকোনো একটি টাইপ** হতে পারে।

## 📌 1. Basic Union

```ts
let data: string | number;

data = "Polok";
data = 25;
```

---

## 📌 2. Union in Function Parameters

```ts
function printId(id: string | number) {
  console.log("Your ID:", id);
}

printId(101);
printId("ABC-123");
```

---

## 📌 3. Union with Arrays

```ts
let values: (string | number)[] = ["polok", 10, 20, "hello"];
```

---

## 📌 4. Union in Type Alias

```ts
type Status = "success" | "error" | "pending";

let orderStatus: Status = "success";
```

---

## 📌 5. Union with Objects

```ts
type Student = {
  name: string;
  class: number;
};

type Teacher = {
  name: string;
  subject: string;
};

let person: Student | Teacher;

person = { name: "Salman", class: 10 };
person = { name: "Islam", subject: "Math" };
```

---

# ⭐ INTERSECTION TYPE ( & )

Intersection মানে — **দুই বা তার বেশি টাইপ মিলে নতুন টাইপ তৈরি হবে**
মানে properties: **সবগুলা থাকতে হবে**।

---

# 📌 1. Basic Intersection

```ts
type A = { name: string };
type B = { age: number };

type Person = A & B;

const p: Person = {
  name: "Polok",
  age: 22
};
```

---

# 📌 2. Intersection with Multiple Types

```ts
type X = { a: string };
type Y = { b: number };
type Z = { c: boolean };

type Combine = X & Y & Z;

const obj: Combine = {
  a: "Hello",
  b: 10,
  c: true
};
```

---

# 📌 3. Intersection with Union (Important)

এটা একটু tricky:

```ts
type A = { name: string };
type B = { age: number };
type C = A | B;    // union

// Intersection with union
type D = C & { id: number };
```

👉 Final result হবে:

* Either (A + id)
* Or (B + id)

---

# 📌 4. Intersection with Function Types

```ts
type Func1 = (a: number) => number;
type Func2 = (b: number) => number;

type MixedFunc = Func1 & Func2;
```

---

# 📌 5. Intersection in Real-life Example

### Example: User + Address

```ts
type User = {
  username: string;
  email: string;
};

type Address = {
  city: string;
  country: string;
};

type UserProfile = User & Address;

const profile: UserProfile = {
  username: "polok",
  email: "polok@gmail.com",
  city: "Dhaka",
  country: "Bangladesh"
};
```

---

# ⭐ UNION vs INTERSECTION — Difference

| Feature     | UNION ( | )      | INTERSECTION ( & ) |
| ----------- | ---------------- | ------------------ |
| মানে        | যেকোনো একটি type | সবগুলো type একত্রে |
| Properties  | কম থাকতে পারে    | সব mandatory       |
| flexibility | বেশি flexible    | strict             |
| Example     | string | number  | A & B              |

---

# 🎯 সহজ ভাষায়

### ✔ **Union ( | )**

"এটা অথবা ওটা"
(Loose / Flexible)

### ✔ **Intersection ( & )**

"সব একসাথে লাগবে"
(Strict / Combined)

---
