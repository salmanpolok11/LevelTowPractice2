# ✅ TypeScript চালানোর জন্য প্রয়োজনীয় বাংলা নোট

### **1️⃣ TypeScript রান করতে “Node.js” লাগে (NVM ব্যবহার করে install করা সহজ)**

TypeScript কোড চলবে Node.js দিয়ে, তাই প্রথমে Node install করতে হবে।
**NVM** হলো Node-এর version control tool — মানে যেকোনো version install, পরিবর্তন, remove করা যায়।

---

### **2️⃣ Node version চেক করার কমান্ড**

```
node -v
```

👉 বর্তমানে কোন Node version চলছে তা দেখাবে।

---

### **3️⃣ NVM দিয়ে নতুন Node version install করার কমান্ড**

```
nvm install 24.11.0
```

👉 এটি Node.js version **24.11.0** install করবে।

---

### **4️⃣ কোন কোন Node version install আছে তা দেখার কমান্ড**

```
nvm ls
```

👉 তোমার PC–তে ইন্সটল হওয়া সব Node version দেখাবে।

---

### **5️⃣ নির্দিষ্ট Node version সক্রিয় (use) করার কমান্ড**

```
nvm use 24.11.0
```

👉 এটি Node version **24.11.0** active করবে।


