

# ✅ TypeScript Primitive Data Types (উদাহরণসহ)


## **1️⃣ string**

টেক্সট বা লেখা বোঝাতে ব্যবহৃত হয়।

```ts
let name: string = "Salman";
```

---

## **2️⃣ number**

যে কোন সংখ্যা—ইন্টিজার, ফ্লোট, নেগেটিভ, পজিটিভ সব।

```ts
let age: number = 21;
let price: number = 199.99;
```

---

## **3️⃣ boolean**

true বা false মান।

```ts
let isActive: boolean = true;
let isAdmin: boolean = false;
```

---

## **4️⃣ null**

ইচ্ছা করে "empty" মান সেট করা।

```ts
let data: null = null;
```

---

## **5️⃣ undefined**

যে ভ্যারিয়েবলে মান দেওয়া হয়নি।

```ts
let x: undefined;
console.log(x); // undefined
```

---

## **6️⃣ symbol**

ইউনিক মান তৈরি করার জন্য ব্যবহৃত বিশেষ টাইপ।

```ts
let id1 = Symbol("id");
let id2 = Symbol("id");
console.log(id1 === id2); // false
```

---

## **7️⃣ bigint**

খুব বড় সংখ্যা সংরক্ষণ করার জন্য।

```ts
let big: bigint = 123456789012345678901234567890n;
```

---

## **8️⃣ any** (not recommended)

যে কোন টাইপ হতে পারে — TypeScript চেক করবে না।

```ts
let value: any = "Hello";
value = 10;   // allowed
value = true; // allowed
```

---

## **9️⃣ unknown**

any-এর মতো, কিন্তু একটু নিরাপদ — ব্যবহার করার আগে টাইপ চেক করতে হয়।

```ts
let data2: unknown = "Text";

if (typeof data2 === "string") {
  console.log(data2.toUpperCase());
}
```

---

## **🔟 never**

যেসব ফাংশন কখনো return করে না।

```ts
function throwError(msg: string): never {
  throw new Error(msg);
}
```

---

# 📌 Bonus: Primitive নয় কিন্তু অনেক ব্যবহৃত টাইপ

(Primitive না হলেও জানা জরুরি)

* **array**

```ts
let list: number[] = [1, 2, 3];
```

* **tuple**

```ts
let user: [string, number] = ["Salman", 21];
```

* **object**

```ts
let person = { name: "Polok", age: 21 };
```
