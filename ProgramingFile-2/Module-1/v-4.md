
## 🎯 TypeScript Non-Primitive Data Types

*(যেগুলো primitive নয়, মান নয় বরং মানের address/reference ধরে)*

---

# **1️⃣ Object**

Key–value আকারে ডেটা রাখে।

```ts
let person: { name: string; age: number } = {
  name: "Salman",
  age: 21
};
```

---

# **2️⃣ Array**

একই টাইপ বা মিশ্র টাইপের অনেকগুলো ভ্যালু লিস্ট আকারে রাখে।

```ts
let nums: number[] = [1, 2, 3];
let mixed: (string | number)[] = ["Salman", 20];
```

---

# **3️⃣ Tuple**

Fixed-length array, যেখানে প্রতিটি পজিশনের টাইপ নির্দিষ্ট থাকে।

```ts
let user: [string, number, boolean] = ["Polok", 21, true];
```

---

# **4️⃣ Function**

ফাংশনও TypeScript-এ একটি data type।

```ts
let add: (a: number, b: number) => number = (a, b) => a + b;
```

---

# **5️⃣ Class**

Object তৈরি করার নকশা (blueprint)।

```ts
class Car {
  model: string;
  constructor(m: string) {
    this.model = m;
  }
}

let bmw = new Car("BMW M5");
```

---

# **6️⃣ Interface**

Object-এর structure নির্ধারণ করে (কোনো কোড জেনারেট করে না)।

```ts
interface User {
  name: string;
  age: number;
}

let u: User = { name: "Salman", age: 21 };
```

---

# **7️⃣ Type Alias**

Custom টাইপ বানাতে ব্যবহার হয়।

```ts
type ID = string | number;
let userId: ID = 101;
```

---

# **8️⃣ Enum**

নামের মাধ্যমে একটা সেট ভ্যালু ডিফাইন করে।

```ts
enum Role {
  Admin,
  User,
  Guest
}

let myRole: Role = Role.Admin;
```

---

# **9️⃣ Map**

Key–value data structure (object এর modern form)।

```ts
let map = new Map();
map.set("name", "Salman");
map.set("age", 21);
```

---

# **🔟 Set**

Unique value-storing data structure।

```ts
let items = new Set([1, 2, 3, 3]);
console.log(items); // {1, 2, 3}
```

---

# **1️⃣1️⃣ WeakMap**

Only object keys অনুমোদিত, garbage collection friendly।

```ts
let wm = new WeakMap();
let obj = {};
wm.set(obj, "data");
```

---

# **1️⃣2️⃣ WeakSet**

Object collection, garbage-collectable।

```ts
let ws = new WeakSet();
let o = {};
ws.add(o);
```

---

# **1️⃣3️⃣ Promise**

Async অপারেশনের future value ধরে।

```ts
let task = new Promise((resolve, reject) => {
  resolve("Done!");
});
```

---

# **1️⃣4️⃣ Date**

তারিখ ও সময় ম্যানিজ করার টাইপ।

```ts
let today = new Date();
```

---

# **15️⃣ RegExp**

Regular Expression pattern।

```ts
let pattern = /salman/i;
```

---

# 📌 Non-Primitive Types Summary Table

| টাইপ      | কাজ                   |
| --------- | --------------------- |
| Object    | Key–value storage     |
| Array     | Value list            |
| Tuple     | Fixed structured list |
| Function  | Callable code         |
| Class     | Object blueprint      |
| Interface | Object structure rule |
| Type      | Custom type           |
| Enum      | Named constant set    |
| Map       | Key-value structure   |
| Set       | Unique values         |
| WeakMap   | Object-key map        |
| WeakSet   | Object set            |
| Promise   | Async result          |
| Date      | Time / date           |
| RegExp    | Pattern matching      |

---
## Typescript Practice
```ts
const bazerList : string[] = [ "Sugar" , "Chal" , 'Dhal' , "Methy"];
bazerList.push("Oil")
// console.log(bazerList);

const mixdArry : ( string | number | boolean) [] =  ["Salman" , 24];
mixdArry.push(true)
// console.log(mixdArry);

// Tuple
const couple : [string , number , boolean] = ["Husbend" ,12 , true];
// console.log(couple);


class Car {
  model: string;
  constructor(model: string) {
    this.model = model;
  }
}

let bmw = new Car("BMW");
let audi = new Car("Audi");

```


