# 🔥 **TypeScript Object—Complete Discussion (Beginner → Advanced)**

# ✅ **1. Object কি?**

Object হলো
👉 এক বা একাধিক property (key–value pair) এর জিনিস।

### উদাহরণ:

```ts
const user = {
  name: "Salman",
  age: 21,
};
```

---

# ✅ **2. Object এর টাইপ কিভাবে declare করো?**

TypeScript-এ তুমি object-কে **type** দিতে পারো।

### A) Inline Type

```ts
const user: { name: string; age: number } = {
  name: "Salman",
  age: 21,
};
```

### B) Type Alias

```ts
type User = {
  name: string;
  age: number;
};

const u: User = { name: "Polok", age: 22 };
```

### C) Interface

```ts
interface User {
  name: string;
  age: number;
}

const p: User = { name: "Islam", age: 25 };
```

**Type Alias এবং Interface—object টাইপ declare করার দুইটা প্রধান উপায়।**

---

# ✅ **3. Optional Property (?)**

যদি কোনো property থাকা বা না-থাকা দুইটাই চলে:

```ts
type User = {
  name: string;
  age?: number;
};

const u: User = { name: "Salman" };
```

---

# ✅ **4. Readonly Property**

একবার সেট করে দিলে আর পরিবর্তন করা যাবে না।

```ts
type User = {
  readonly id: number;
  name: string;
};

const person: User = { id: 1, name: "Salman" };

// person.id = 5 ❌  (error)
```

---

# ✅ **5. Object Destructuring**

Object থেকে মান আলাদা করে ভেরিয়েবলে নেওয়া।

```ts
const user = { name: "Salman", age: 21 };

const { name, age } = user;
console.log(name);
```

---

# ✅ **6. Nested Object**

Object-এর ভিতরে object।

```ts
type User = {
  name: string;
  address: {
    city: string;
    zip: number;
  };
};

const user: User = {
  name: "Salman",
  address: { city: "Dhaka", zip: 1230 },
};
```

---

# ✅ **7. Index Signature**

যখন property নাম আগেই জানা নেই:

```ts
type Data = {
  [key: string]: string;
};

const info: Data = {
  name: "Salman",
  country: "Bangladesh",
};
```

---

# ✅ **8. Union Type Object**

যদি object দুই–তিন ধরনের হতে পারে:

```ts
type Dog = { type: "dog"; bark: () => void };
type Cat = { type: "cat"; meow: () => void };

type Pet = Dog | Cat;
```

---

# ✅ **9. Intersection Type Object**

দুইটা টাইপ merge করা।

```ts
type A = { name: string };
type B = { age: number };

type C = A & B;

const person: C = {
  name: "Salman",
  age: 21,
};
```

---

# ✅ **🔟 Function inside Object (Methods)**

```ts
const obj = {
  name: "Salman",
  greet() {
    console.log("Hello!");
  }
};

obj.greet();
```

---

# ✅ **1️⃣1️⃣ Object as Parameter**

```ts
function printUser(user: { name: string; age: number }) {
  console.log(user.name);
}

printUser({ name: "Polok", age: 22 });
```

---

# ✅ **1️⃣2️⃣ Object Array**

Object-এর লিস্ট/collection।

```ts
type Product = { id: number; name: string; price: number };

const products: Product[] = [
  { id: 1, name: "Phone", price: 10000 },
  { id: 2, name: "Laptop", price: 50000 },
];
```

---

# ✅ **1️⃣3️⃣ keyof Operator**

Object-এর সব key নিয়ে type বানানো।

```ts
type User = { name: string; age: number };

type UserKeys = keyof User; // "name" | "age"
```

---

# ✅ **1️⃣4️⃣ typeof Operator**

কোনো object-এর টাইপ বের করা।

```ts
const user = { name: "Salman", age: 21 };

type UserType = typeof user;
```

---

# ✅ **1️⃣5️⃣ Partial / Required / Pick / Omit (Utility Types)**

TypeScript কিছু built-in helper দেয়:

### ✔ Partial → সব property optional করে দেয়

```ts
type User = { name: string; age: number };
let u: Partial<User> = { name: "Salman" };
```

### ✔ Required → সব property required করে দেয়

### ✔ Pick → কিছু property বেছে নেয়

### ✔ Omit → কিছু property বাদ দেয়

---

# ✅ **1️⃣6️⃣ Object.freeze()**

Object immutable করে।

```ts
const user = Object.freeze({
  id: 1,
  name: "Salman"
});

// user.name = "X" ❌ (not allowed)
```

---

# 🌟 **TypeScript Object কোথায় ব্যবহার হয় (Real Life Examples)**

| ক্ষেত্র         | Object ব্যবহার            |
| --------------- | ------------------------- |
| 🔥 API Response | Server থেকে আসা JSON data |
| 🔥 User info    | name, email, token        |
| 🔥 Product list | e-commerce                |
| 🔥 Config file  | environment settings      |
| 🔥 Game dev     | player stats, enemies     |
| 🔥 Dashboard    | charts, analytics, stats  |

---

## Practice Object Code
```ts
const user : {
     fristName : string,
     middeleName? : string
     lastName : string
} = {
     fristName : "Salman",
     
     lastName : "Polok"
}

console.log(user);

```