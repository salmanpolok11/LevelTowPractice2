## **১. Function কি?**

Function হল কোডের একটি ব্লক যা কোনো নির্দিষ্ট কাজ সম্পাদন করে। আমরা একবার function লিখে বারবার ব্যবহার করতে পারি।

TypeScript-এ function লিখার মূল রূপ (syntax):

```ts
function functionName(parameter1: type, parameter2: type): returnType {
    // function body
    return something;
}
```

---

## **২. Function এর ধরন**

### ১) Named Function

সবচেয়ে সাধারণ ধরন:

```ts
function add(a: number, b: number): number {
    return a + b;
}

console.log(add(5, 3)); // 8
```

**বিঃদ্রঃ**

* `a: number` → parameter টাইপ
* `: number` → return টাইপ

---

### ২) Anonymous Function

নাম ছাড়া function:

```ts
const multiply = function(a: number, b: number): number {
    return a * b;
}

console.log(multiply(4, 5)); // 20
```

---

### ৩) Arrow Function

ES6 থেকে নতুন ধরন। compact এবং modern:

```ts
const divide = (a: number, b: number): number => {
    return a / b;
}

console.log(divide(10, 2)); // 5
```

**Compact version (single expression):**

```ts
const square = (x: number): number => x * x;
console.log(square(6)); // 36
```

---

### ৪) Optional Parameters

কিছু parameter দিতে না চাইলে:

```ts
function greet(name: string, greeting?: string) {
    if(greeting) {
        console.log(`${greeting}, ${name}`);
    } else {
        console.log(`Hello, ${name}`);
    }
}

greet("Salman"); // Hello, Salman
greet("Salman", "Good Morning"); // Good Morning, Salman
```

**বিঃদ্রঃ**

* `?` দিয়ে parameter optional করা যায়।

---

### ৫) Default Parameters

Parameter এর default value দিতে পারি:

```ts
function power(base: number, exponent: number = 2): number {
    return base ** exponent;
}

console.log(power(5)); // 25
console.log(power(2, 3)); // 8
```

---

### ৬) Rest Parameters

অজস্র parameter handle করতে:

```ts
function sum(...numbers: number[]): number {
    return numbers.reduce((acc, curr) => acc + curr, 0);
}

console.log(sum(1, 2, 3, 4)); // 10
```

---

### ৭) Function Overloading (TypeScript Specific)

একই function কে বিভিন্নভাবে ব্যবহার করা যায়।

```ts
function combine(a: string, b: string): string;
function combine(a: number, b: number): number;

function combine(a: any, b: any): any {
    return a + b;
}

console.log(combine(5, 10)); // 15
console.log(combine("Hello, ", "World")); // Hello, World
```

---

### ৮) Void Function

কোনো value return না করলে:

```ts
function logMessage(message: string): void {
    console.log(message);
}

logMessage("TypeScript is fun!");
```

---

### ৯) Never Function

যে function কখনো return করে না (যেমন error throw):

```ts
function throwError(message: string): never {
    throw new Error(message);
}
```

---

## **১০) Function Type Declaration**

TypeScript-এ function কে variable হিসেবে type define করা যায়:

```ts
let myFunc: (a: number, b: number) => number;

myFunc = (x, y) => x + y;

console.log(myFunc(7, 8)); // 15
```

---

💡 **Summary Table**

| ধরন            | উদাহরণ                   | কী ব্যবহার               |                            |
| -------------- | ------------------------ | ------------------------ | -------------------------- |
| Named          | `function add(a,b){}`    | সাধারণ function          |                            |
| Anonymous      | `const f = function(){}` | variable assign          |                            |
| Arrow          | `const f = () => {}`     | compact & modern         |                            |
| Optional Param | `greet(name?: string)`   | optional value           |                            |
| Default Param  | `power(a, b=2)`          | default value            |                            |
| Rest Param     | `sum(...nums)`           | multiple args            |                            |
| Overloading    | `combine(a:string        | number)`                 | same name, different types |
| Void           | `function f():void`      | return না করা            |                            |
| Never          | `function f():never`     | always throw বা infinite |                            |

---


// Practice funtion 
```ts
// Normal function

function normalFuntion (num1: number , num2 : number) : number{
     const result = num1 + num2
     return result;
}
const run = normalFuntion(2 ,2)
// console.log(run);


// arrow Funtion 

const arrFunction = ( num3: number , num4: number): number =>{
   const store = num3 + num4;
   return store
}

console.log(arrFunction(5,5));
```
// object ar modde medhod 
```ts
const poorUser = {
     name: "Polok",
     age : 24,
     balance : 0,
     addBlance ( value : number) : number {
        //   const totalBalce = this.balance + value;
          this.balance = this.balance + value;
     return this.balance;

     }
}

console.log(poorUser.addBlance(500));


```