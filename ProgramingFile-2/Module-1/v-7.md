# 🔥 **Spread Operator (…) — সম্পূর্ণ ব্যাখ্যা**

Spread operator (`...`) ব্যবহার করা হয়:

* array copy করতে
* array merge করতে
* object copy করতে
* object merge/update করতে
* function parameter pass করতে (rest parameter বিপরীত)

---

# 🟦 ১) **Array এ Spread Operator**

## ✔ Array Copy

```ts
const nums = [1, 2, 3];
const copyNums = [...nums];

console.log(copyNums); // [1, 2, 3]
```

---

## ✔ Array Merge

```ts
const a = [1, 2];
const b = [3, 4];

const merged = [...a, ...b];

console.log(merged); // [1,2,3,4]
```

---

## ✔ Array এ নতুন value যোগ করা

```ts
const arr = [2, 3, 4];
const newArr = [1, ...arr, 5];

console.log(newArr); // [1,2,3,4,5]
```

---

# 🟦 ২) **Object এ Spread Operator**

## ✔ Object Copy

```ts
const user = { name: "Polok", age: 24 };
const copyUser = { ...user };

console.log(copyUser);
```

---

## ✔ Object Merge

```ts
const address = { city: "Dhaka" };
const info = { name: "Polok", age: 24 };

const userInfo = { ...info, ...address };

console.log(userInfo);
// { name: "Polok", age: 24, city: "Dhaka" }
```

---

## ✔ Object Update (immutably)

```ts
const user = { name: "Polok", age: 24 };

const updated = {
  ...user,
  age: 25,   // overwrite করা হল
};

console.log(updated);  // {name:"Polok", age:25}
```

---

# 🟦 ৩) **Spread in Function Arguments**

```ts
function add(a: number, b: number, c: number): number {
     return a + b + c;
}

const values = [2, 3, 4];

console.log(add(...values)); // 9
```

---

# 🟦 ৪) **Rest Parameter (Spread এর বিপরীত)**

Rest = function parameter-এ multiple value ধরার জন্য।

```ts
function sum(...numbers: number[]): number {
     return numbers.reduce((acc, n) => acc + n, 0);
}

console.log(sum(1, 2, 3, 4)); // 10
```

⚠️ মনে রাখবে:

* **Function parameter এ → rest (…)**
* **Array/Object side এ → spread (…)**

---

# 🟦 ৫) **TypeScript-এ Spread + Type একসাথে**

## ✔ Array type

```ts
const arr: number[] = [1, 2, 3];
const newArr: number[] = [...arr, 4];
```

---

## ✔ Object type

```ts
type User = {
     name: string;
     age: number;
};

const u1: User = { name: "Polok", age: 24 };

const u2: User = { ...u1, age: 25 };
```

---

# 🟦 ৬) **Spread Operator + Destructuring**

## ✔ Array destructuring with rest

```ts
const [first, ...rest] = [1, 2, 3, 4];

console.log(first); // 1
console.log(rest);  // [2,3,4]
```

---

## ✔ Object destructuring with rest

```ts
const user = { name: "Polok", age: 24, city: "Dhaka" };

const { name, ...others } = user;

console.log(name);   // Polok
console.log(others); // {age:24, city:"Dhaka"}
```

---

# 🟦 ৭) **Spread Operator দিয়ে Immutable Programming**

React, Redux, TS Libs এ সবচেয়ে বেশি ব্যবহৃত:

```ts
const state = { count: 0 };

const newState = { ...state, count: state.count + 1 };

console.log(newState); // {count:1}
```

---

# 🟦 ৮) **Spread দিয়ে Deep Copy হয় না (Important!)**

```ts
const obj = { user: { name: "Polok" } };

const copy = { ...obj };

copy.user.name = "Rahim";

console.log(obj.user.name); // Rahim ❗
```

Spread শুধু **shallow copy** করে।

---

# 🎯 **সংক্ষেপে সব কিছু:**

| কাজ                 | Spread | Rest |
| ------------------- | ------ | ---- |
| Array copy          | yes    | no   |
| Array merge         | yes    | no   |
| Object copy         | yes    | no   |
| Function arguments  | yes    | no   |
| Function parameters | no     | yes  |
| Destructuring       | yes    | yes  |

---
Spread Operate opretor practice 
```ts
const frieds = [ "Shamim" , 'Sujon' , "Maruf"]
const collageFriends = [ "Bonni" , "Himel" , "Maliha" , "Andresh"];
const uniVercityFriends = [ "Jubayer" , "faize" , "Ashik" , "Monsur"]

frieds.push(...collageFriends)
console.log(frieds);
frieds.push(...uniVercityFriends)
console.log(frieds);

```