

# ⭐ Type Alias — TypeScript এ সম্পূর্ণ গাইড

`type` keyword ব্যবহার করে আমরা যেকোনো **custom type** define করতে পারি।
এটি কোডকে clean, reusable এবং readable করে তোলে।

---

# ✅ 1. Basic Type Alias

যখন একটি value বা variable-এর জন্য custom type বানাতে চাও।

### ✔ Example:

```ts
type Age = number;

let userAge: Age = 25;
```

---

# ✅ 2. Object Type Alias

Object-এর shape/type আগে থেকে define করে রাখার জন্য ব্যবহার করা হয়।

### ✔ Example:

```ts
type Person = {
  name: string;
  age: number;
  married: boolean;
};

const user: Person = {
  name: "Polok",
  age: 25,
  married: false
};
```

---

# ✅ 3. Nested Object Type Alias

Object এর ভিতরে object থাকলে।

### ✔ Example:

```ts
type FullName = {
  firstName: string;
  middleName?: string;  // optional
  lastName: string;
};

type User = {
  name: FullName;
  age: number;
  married: boolean;
};

const info: User = {
  name: {
    firstName: "Salman",
    lastName: "Polok"
  },
  age: 25,
  married: true
};
```

---

# ✅ 4. Function Type Alias

Function-এর parameter ও return type define করা।

### ✔ Example:

```ts
type Add = (a: number, b: number) => number;

const sum: Add = (x, y) => x + y;

console.log(sum(10, 20));
```

---

# ✅ 5. Union Type Alias

যখন একাধিক type allow করতে চাও।

### ✔ Example:

```ts
type ID = number | string;

let userId: ID = 101;
userId = "abc-123";
```

---

# ✅ 6. Intersection Type Alias (&)

দুই বা তার বেশি type merge করে create করা।

### ✔ Example:

```ts
type A = { name: string };
type B = { age: number };

type PersonDetails = A & B;

let p: PersonDetails = {
  name: "Polok",
  age: 25
};
```

---

# ✅ 7. Array Type Alias

Array-এর জন্য custom structure define করা।

### ✔ Example:

```ts
type Numbers = number[];

let list: Numbers = [1, 2, 3];
```

---

# ✅ 8. Tuple Type Alias

Tuple specific order এবং fixed length।

### ✔ Example:

```ts
type UserTuple = [string, number, boolean];

const t: UserTuple = ["Polok", 25, true];
```

---

# ✅ 9. Optional Properties

যে property থাকতেও পারে, নাও থাকতে পারে।

### ✔ Example:

```ts
type Student = {
  name: string;
  class?: number;  // optional
};
```

---

# ✅ 10. Readonly Properties

যে properties পরে পরিবর্তন করা যাবে না।

### ✔ Example:

```ts
type Config = {
  readonly apiKey: string;
};

const settings: Config = {
  apiKey: "123abc"
};

// settings.apiKey = "456def"; // ❌ error
```

---

# 🎯 Bonus: Type Alias vs Interface (Short)

| Feature                      | Type Alias | Interface |
| ---------------------------- | ---------- | --------- |
| Object Description           | ✔          | ✔         |
| Function/Union/Tuple support | ✔          | ❌         |
| Can extend others            | ✔          | ✔         |
| More flexible                | ✔          | —         |

---

# 📌 Summary

Type Alias ব্যবহার করে তুমি করতে পারো—

✔ Object type define
✔ Function type define
✔ Union / Intersection
✔ Tuple
✔ Array
✔ Nested object
✔ Optional & Readonly

এটি TypeScript এর সবচেয়ে শক্তিশালী feature গুলোর একটি।

---
